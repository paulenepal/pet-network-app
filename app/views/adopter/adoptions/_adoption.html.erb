<tr id="<%= dom_id(adoption) %>">
  <td><%= adoption.adopter_id %></td>
  <td><%= adoption.pet_id %></td>
  <td><%= adoption.status %></td>
  <td>
    <%= link_to 'Show', adopter_adoption_path(adoption) %>
    <%= link_to 'Destroy', adopter_adoption_path(adoption), method: :delete, data: { confirm: 'Are you sure?' } %>
  </td>
</tr>
